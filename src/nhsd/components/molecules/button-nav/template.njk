{#- Import atom macros -#}
{%- from "../../../njk/macros/atoms.njk" import
  nhsd_a_button
-%}

{# Set classes #}
{%- set classNames = "nhsd-m-button-nav" -%}
{%- if params.classes -%}
  {% set classNames = classNames + " " + params.classes %}
{%- endif -%}

{% if params.buttons|length -%}
<nav class="{{ classNames }}">
{% for button in params.buttons -%}
  {{ nhsd_a_button(button) }}
{% endfor -%}
</nav>
{% endif -%}

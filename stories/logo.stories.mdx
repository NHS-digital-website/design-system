import * as sbConfig from './storybook.config.js';

import { Canvas, Story, Meta } from '@storybook/addon-docs/blocks';
import template from  '../src/components/atoms/logo/template.njk';
require('../src/components/atoms/logo/_index.scss');

<Meta title={sbConfig.title + '/' + sbConfig.componentsTitle + '/' + sbConfig.atomsTitle + '/ Logo'} />

## Logo source

<Canvas>
  <Story
    name="NHS Digital logo"
    height="100px"
    parameters={{
      docs: {
        source: {
          code : template.render()
        },
      }
    }}
    args={{ className: 'hello-world' }} argTypes={{ className: { control: 'text' } }}
  >
    { template.render() }
  </Story>
</Canvas>